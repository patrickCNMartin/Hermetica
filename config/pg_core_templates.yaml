# yaml file for protocol templates
# This is a simple way to bootstrap the data base.
# The structure will pull from the a live protocol database if one exists.
# I am keeping this pretty minimal because it should also be able to be updated
# Using a specific manifest hash.
#
# `nodes` says which protocol each step runs; `dag` wires the steps together.
# Node ids are the pipeline's own, so the same protocol may run at more than one
# step. The graphs below are still placeholders and will not load.


creator: "Homunculus Pat"
created_on: "2026-08-21"
pipelines:
    CryPrep_biomek_base:
        pipeline_guid: null
        root: null
        nodes: {A: "A", B: "B", C: "C", D: "D"}
        dag: {A: ["B", "C"], B: ["D"], C: ["D"], D: []}

    FFPE_digestion_base:
        pipeline_guid: null
        root: null
        nodes: {}
        dag: {}

    In_gel_digestion_base:
        pipeline_guid: null
        root: null
        nodes: {}
        dag: {}

    IP_base:
        pipeline_guid: null
        root: null
        nodes: {}
        dag: {}

    IPMS_base:
        pipeline_guid: null
        root: null
        nodes: {}
        dag: {}

    FF_SDS_base:
        pipeline_guid: null
        root: null
        nodes: {}
        dag: {}

    Frozen_tissue_base:
        pipeline_guid: null
        root: null
        nodes: {}
        dag: {}
